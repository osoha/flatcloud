import Link from "next/link";
import type { loadGeneralKpis } from "@/lib/reporting/general-kpis";
import { money, date } from "@/lib/format";
export function GeneralKpis({data}:{data:Awaited<ReturnType<typeof loadGeneralKpis>>}) {
  const amount=(value:number|null)=>value===null?"—":money(value), percent=(value:number|null)=>value===null?"—":`${(value/100).toLocaleString("cs-CZ",{maximumFractionDigits:2})} %`;
  if(!data.includedCount)return <section className="card"><h2>KPIs nemovitostí</h2><p>Vyberte celý objekt, ke kterému máte přístup. Výnosové ukazatele potřebují jeho nájemné, náklady a ocenění; z údajů jediné jednotky nelze hodnotit celý dům.</p></section>;
  return <><div className="stat-grid v21-stat-grid">{[["NOI · roční přepočet",amount(data.noiCents)],["Čistý výnos (Yield)",percent(data.yieldBps)],["Výnos vlastního kapitálu (ROE)",percent(data.roeBps)],["Hodnota nemovitostí",amount(data.marketValueCents)],["Evidovaná jistina úvěrů",data.principalComplete?amount(data.outstandingPrincipalCents):"—"],["Zadlužení (LTV)",percent(data.ltvBps)]].map(([name,value])=><div className="card stat" key={name}><div><span>{name}</span><strong>{value}</strong></div></div>)}</div><section className="card"><h2>Jak číst ukazatele</h2><p className="metric-definition">NOI = aktuální měsíční čisté nájemné × 12 − evidované provozní náklady za posledních 12 měsíců. Yield = NOI / poslední hodnota nemovitosti. ROE zde vyjadřuje peněžní výnos: (NOI − roční splátky úvěrů) / (hodnota − jistina). LTV = jistina / hodnota. Změna tržní ceny se do ROE nezapočítává.</p><p className="metric-definition">Výsledky odpovídají úplnosti vaší evidence. Bez nákladů nebo ocenění příslušné ukazatele nedopočítáváme; pomlčka není nula. Nejsou-li úvěry evidovány, výpočet předpokládá financování bez dluhu. Úvěr bez doloženého stavu zablokuje ukazatele závislé na financování.</p></section><section className="card portfolio-table-card"><div className="table-toolbar"><h2>KPIs podle nemovitostí</h2></div><div className="table-wrap"><table><thead><tr><th>Nemovitost</th><th>NOI</th><th>Yield</th><th>ROE</th><th>Hodnota</th><th>Oceněno k</th><th>Podklady</th></tr></thead><tbody>{data.propertyRows.map(row=><tr key={row.propertyId}><td><Link href={`/nemovitosti/${row.propertyId}/finance`}>{row.propertyName}</Link></td><td>{amount(row.noiCents)}</td><td>{percent(row.yieldBps)}</td><td>{percent(row.roeBps)}</td><td>{amount(row.marketValueCents)}</td><td>{row.valuationAsOfDate?date(row.valuationAsOfDate):"—"}</td><td>{[!row.opexComplete&&"Doplnit provozní náklady",row.marketValueCents===null&&"Doplnit ocenění",!row.principalComplete&&"Doplnit stav úvěru",row.annualDebtServiceCents===null&&"Doplnit splátky"].filter(Boolean).join(" · ")||"Evidované podklady dostupné"}</td></tr>)}</tbody></table></div></section></>;
}
