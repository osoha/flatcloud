export function UnitConditionExecutionForm({ propertyId, unitId, assessmentId, unitLabel, returnTo }: { propertyId: string; unitId: string; assessmentId: string; unitLabel: string; returnTo: string }) {
  return <form className="condition-execution-form" action={`/api/properties/${propertyId}/units/${unitId}/condition-assessments/${assessmentId}/execute`} method="post"><input type="hidden" name="returnTo" value={returnTo}/><label className="field"><span>Název CAPEX akce *</span><input name="title" defaultValue={`Obnova jednotky ${unitLabel}`} maxLength={160} required/></label><div className="execution-preview"><span>Vznikne úkol údržby</span><span>Plánovaný CAPEX náklad</span><span>Rozpočtová položka pro rok cílového termínu</span></div><button className="primary" type="submit">Převést do realizace</button></form>;
}
