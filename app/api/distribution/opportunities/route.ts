import {DistributionOpportunityStage} from "@prisma/client";import {requireUser} from "@/lib/auth";import {dateValue,text} from "@/lib/forms";import {createDistributionOpportunity} from "@/lib/distribution/crm";import {goWithMessage} from "@/lib/route-response";
function optionalMoney(form:FormData,key:string){const raw=String(form.get(key)||"").trim().replace(/\s/g,"").replace(",",".");if(!raw)return null;const value=Number(raw);if(!Number.isFinite(value))throw new Error(`Neplatná částka ${key}.`);return Math.round(value*100)}
export async function POST(request:Request){const user=await requireUser();try{const form=await request.formData();await createDistributionOpportunity(user,{prospectId:text(form,"prospectId",true)!,unitId:text(form,"unitId",true)!,stage:String(form.get("stage")||"NEW") as DistributionOpportunityStage,askingPriceCents:optionalMoney(form,"askingPrice"),offeredPriceCents:optionalMoney(form,"offeredPrice"),nextActionAt:dateValue(form,"nextActionAt"),note:text(form,"note")});return goWithMessage(request,"/distribuce/zajemci","ok","Příležitost byla založena.")}catch(error){return goWithMessage(request,"/distribuce/zajemci","error",error instanceof Error?error.message:"Příležitost se nepodařilo založit.")}}
