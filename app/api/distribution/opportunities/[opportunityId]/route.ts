import {DistributionOpportunityStage} from "@prisma/client";import {requireUser} from "@/lib/auth";import {dateValue,text} from "@/lib/forms";import {updateDistributionOpportunity} from "@/lib/distribution/crm";import {goWithMessage} from "@/lib/route-response";
function optionalMoney(form:FormData,key:string){const raw=String(form.get(key)||"").trim().replace(/\s/g,"").replace(",",".");if(!raw)return null;const value=Number(raw);if(!Number.isFinite(value))throw new Error(`Neplatná částka ${key}.`);return Math.round(value*100)}
export async function POST(request:Request,{params}:{params:Promise<{opportunityId:string}>}){const user=await requireUser(),{opportunityId}=await params;try{const form=await request.formData();await updateDistributionOpportunity(user,opportunityId,{stage:String(form.get("stage")) as DistributionOpportunityStage,askingPriceCents:optionalMoney(form,"askingPrice"),offeredPriceCents:optionalMoney(form,"offeredPrice"),nextActionAt:dateValue(form,"nextActionAt"),note:text(form,"note")});return goWithMessage(request,"/distribuce/zajemci","ok","Fáze a další krok byly aktualizovány.")}catch(error){return goWithMessage(request,"/distribuce/zajemci","error",error instanceof Error?error.message:"Příležitost se nepodařilo upravit.")}}
