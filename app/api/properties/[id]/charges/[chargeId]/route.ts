import { prisma } from "@/lib/db";
import { boolValue, dateValue, moneyToCents, text } from "@/lib/forms";
import { requireManagedProperty, audit } from "@/lib/management";
import { go, goWithMessage } from "@/lib/route-response";
export async function POST(request:Request,{params}:{params:Promise<{id:string;chargeId:string}>}){const{id,chargeId}=await params;const access=await requireManagedProperty(id);if(!access)return go(request,"/login");try{const existing=await prisma.charge.findFirst({where:{id:chargeId,lease:{unit:{propertyId:id}}},include:{allocations:true}});if(!existing)throw new Error("Měsíční předpis nebyl nalezen.");const form=await request.formData();const amountCents=moneyToCents(form,"amount");const paid=existing.allocations.reduce((s,a)=>s+a.amountCents,0);if(amountCents<paid)throw new Error("Předpis nelze snížit pod již uhrazenou částku.");const active=boolValue(form,"active");await prisma.charge.update({where:{id:chargeId},data:{amountCents,dueDate:dateValue(form,"dueDate",true)!,note:text(form,"note"),active}});await audit(access.user.id,"CHARGE_UPDATED","Charge",chargeId,{propertyId:id,amountCents,active});return goWithMessage(request,`/nemovitosti/${id}/predpisy/mesicni/${chargeId}`,"ok",active?"Měsíční předpis byl upraven.":"Předpis byl pro tento měsíc vypnut.")}catch(error){return goWithMessage(request,`/nemovitosti/${id}/predpisy/mesicni/${chargeId}`,"error",error instanceof Error?error.message:"Předpis se nepodařilo upravit.")}}
