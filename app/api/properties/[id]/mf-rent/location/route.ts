import {currentUser} from "@/lib/auth";import {assignPropertyMfRentLocation} from "@/lib/reporting/mf-rent/location-service";import {go,goWithMessage} from "@/lib/route-response";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const user=await currentUser();if(!user)return go(request,"/login");const{id}=await params;const form=await request.formData();try{await assignPropertyMfRentLocation(user,id,String(form.get("territoryCode")||""));return goWithMessage(request,`/nemovitosti/${id}/reporting`,"ok","Katastrální území MF bylo přiřazeno.")}catch(error){return goWithMessage(request,`/nemovitosti/${id}/reporting`,"error",error instanceof Error?error.message:"Přiřazení se nezdařilo.")}}
