import {requireUser} from "@/lib/auth";import {deliverSettlement} from "@/lib/service-settlement-workflow";import {text} from "@/lib/forms";import {goWithMessage} from "@/lib/route-response";export async function POST(request:Request,{params}:{params:Promise<{leaseId:string;protocolId:string}>}){const{leaseId,protocolId}=await params,u=await requireUser();try{const f=await request.formData(),raw=text(f,"objectionUntil");await deliverSettlement(u,protocolId,{method:text(f,"method",true)!,reference:text(f,"reference"),objectionUntil:raw?new Date(raw+"T23:59:59Z"):null});return goWithMessage(request,`/smlouvy/${leaseId}/vyuctovani/${protocolId}`,"ok","Doručení bylo zaznamenáno.");}catch(e){return goWithMessage(request,`/smlouvy/${leaseId}/vyuctovani/${protocolId}`,"error",e instanceof Error?e.message:"Doručení selhalo.");}}