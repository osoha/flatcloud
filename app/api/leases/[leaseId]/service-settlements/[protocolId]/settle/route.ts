import {requireUser} from "@/lib/auth";import {settleSettlement} from "@/lib/service-settlement-workflow";import {text} from "@/lib/forms";import {goWithMessage} from "@/lib/route-response";export async function POST(request:Request,{params}:{params:Promise<{leaseId:string;protocolId:string}>}){const{leaseId,protocolId}=await params,u=await requireUser();try{const f=await request.formData(),raw=text(f,"dueDate",true)!;await settleSettlement(u,protocolId,new Date(raw+"T12:00:00Z"));return goWithMessage(request,`/smlouvy/${leaseId}/vyuctovani/${protocolId}`,"ok","Finanční vypořádání bylo vytvořeno.");}catch(e){return goWithMessage(request,`/smlouvy/${leaseId}/vyuctovani/${protocolId}`,"error",e instanceof Error?e.message:"Vypořádání selhalo.");}}