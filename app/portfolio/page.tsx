import Link from "next/link";
import { Building2, Landmark, Plus, WalletCards } from "lucide-react";
import { requireUser, canSeeAll } from "@/lib/auth";
import { accessibleProperties } from "@/lib/access";
import { money } from "@/lib/format";
import { currentPeriod } from "@/lib/period";
import { Shell } from "@/components/Shell";
import { Flash } from "@/components/FormUi";

export const dynamic="force-dynamic";
export default async function Portfolio({searchParams}:{searchParams:Promise<{ok?:string;error?:string}>}){const user=await requireUser();const [properties,query]=await Promise.all([accessibleProperties(user),searchParams]);const period=currentPeriod();const rows=properties.map(property=>{let expected=0,paid=0;for(const unit of property.units)for(const lease of unit.leases)for(const charge of lease.charges.filter(c=>c.period===period)){expected+=charge.amountCents;paid+=charge.allocations.reduce((sum,allocation)=>sum+allocation.amountCents,0)}return{property,expected,paid,balance:paid-expected,rate:expected?Math.round(paid/expected*100):100}});const expected=rows.reduce((s,r)=>s+r.expected,0),paid=rows.reduce((s,r)=>s+r.paid,0);return <Shell user={user}><div className="page"><div className="page-title"><div><h1>Portfolio nemovitostí</h1><p>Souhrn objektů dostupných přihlášenému uživateli · období {period}.</p></div>{canSeeAll(user.role)&&<div className="action-row"><Link className="secondary" href="/vlastnici">Vlastníci / SPV</Link><Link className="primary" href="/nemovitosti/nova"><Plus size={15}/> Přidat nemovitost</Link></div>}</div><Flash ok={query.ok} error={query.error}/><div className="stat-grid"><div className="card stat"><div className="stat-icon blue"><Building2/></div><div><span>Nemovitosti</span><strong>{rows.length}</strong><small>v rozsahu uživatele</small></div></div><div className="card stat"><div className="stat-icon purple"><Landmark/></div><div><span>Vlastníci / SPV</span><strong>{new Set(rows.map(r=>r.property.ownerId)).size}</strong><small>oddělená portfolia</small></div></div><div className="card stat"><div className="stat-icon orange"><WalletCards/></div><div><span>Předpis {period}</span><strong>{money(expected)}</strong><small>všechny dostupné objekty</small></div></div><div className="card stat"><div className="stat-icon green">✓</div><div><span>Uhrazeno</span><strong>{money(paid)}</strong><small className="good">{expected?Math.round(paid/expected*100):100} % inkaso</small></div></div><div className="card stat"><div className="stat-icon red">!</div><div><span>Saldo</span><strong>{money(paid-expected)}</strong><small className="bad">za zvolené období</small></div></div></div><div className="card portfolio-table-card"><div className="table-toolbar"><div><h2>Nemovitosti</h2><p>Kliknutím otevřete samostatný dashboard objektu.</p></div></div><div className="table-wrap"><table><thead><tr><th>Nemovitost</th><th>Vlastník / SPV</th><th>Banka</th><th>Předpis</th><th>Saldo</th><th>Inkaso</th><th>Stav</th></tr></thead><tbody>{rows.length?rows.map(({property,expected,balance,rate})=><tr className="property-row" key={property.id}><td><Link className="property-cell" href={`/nemovitosti/${property.id}/prehled`}><div className="property-thumb">⌂</div><div><strong>{property.name}</strong><small>{property.address}, {property.city}</small></div></Link></td><td><span className="owner-label">{property.owner.name}</span><span className="owner-sub">{property.owner.ico?`IČO ${property.owner.ico}`:"Externí vlastník"}</span></td><td>{property.bankAccounts.find(a=>a.provider!=="manual")?.bankName||"Nepřipojeno"}<span className="owner-sub">{property.bankAccounts.find(a=>a.provider!=="manual")?.ibanMasked}</span></td><td className="money">{money(expected)}</td><td className={balance<0?"money negative":"money positive"}>{money(balance)}</td><td className="collection-cell"><div className="collection-top"><span>{rate}%</span></div><div className={`progress ${rate<85?"bad":rate<95?"warn":""}`}><i style={{width:`${Math.min(rate,100)}%`}}/></div></td><td><span className={`status ${rate<85?"bad":rate<100?"warn":"ok"}`}>{rate===100?"Uhrazeno":rate<85?"Dluh":"Částečně"}</span></td></tr>):<tr><td colSpan={7} className="table-empty"><h3>Zatím nejsou evidované nemovitosti</h3>{canSeeAll(user.role)&&<Link className="primary inline-button" href="/nemovitosti/nova">Přidat první nemovitost</Link>}</td></tr>}</tbody></table></div></div></div></Shell>}
