import {S3Client,PutObjectCommand,DeleteObjectCommand,HeadObjectCommand,GetObjectCommand} from "@aws-sdk/client-s3";import {getSignedUrl} from "@aws-sdk/s3-request-presigner";import type {FileStorage,PutObjectInput} from "./types";
export class S3FileStorage implements FileStorage {private client:S3Client;private bucket:string;constructor(){this.bucket=required("S3_BUCKET");this.client=new S3Client({endpoint:process.env.S3_ENDPOINT||undefined,region:required("S3_REGION"),forcePathStyle:process.env.S3_FORCE_PATH_STYLE==="true",credentials:{accessKeyId:required("S3_ACCESS_KEY_ID"),secretAccessKey:required("S3_SECRET_ACCESS_KEY")}})}async putObject(i:PutObjectInput){await this.client.send(new PutObjectCommand({Bucket:this.bucket,Key:i.key,Body:i.body,ContentType:i.contentType}))}async deleteObject(key:string){await this.client.send(new DeleteObjectCommand({Bucket:this.bucket,Key:key}))}async getSignedDownloadUrl(key:string,expiresSeconds=300){return getSignedUrl(this.client,new GetObjectCommand({Bucket:this.bucket,Key:key}),{expiresIn:expiresSeconds})}async exists(key:string){try{await this.client.send(new HeadObjectCommand({Bucket:this.bucket,Key:key}));return true}catch{return false}}}
function required(name:string){const value=process.env[name];if(!value)throw new Error(`${name} is required for S3 storage.`);return value}
